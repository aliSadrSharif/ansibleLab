---
# Copy to secrets.yml and fill in real values.
# secrets.yml is git-ignored — never commit real credentials.

mongo_root_user: admin
mongo_root_password: change-me
mongo_express_user: admin
mongo_express_password: change-me

# Optional comma-separated CORS allowlist. Leave empty for same-origin via Nginx.
cors_origins: ""
