---
# Main entry point: runs the full server prep + app deploy + nginx/SSL setup, in order.
# Usage: ansible-playbook -i ../02_ansible_setup/inventory site.yml
- import_playbook: ../02_ansible_setup/server_setup.yml
- import_playbook: deploy_app.yml
- import_playbook: deploy_nginx.yml
